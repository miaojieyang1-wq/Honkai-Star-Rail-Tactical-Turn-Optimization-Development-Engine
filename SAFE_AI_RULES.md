# SAFE_AI_RULES

本文件是给 AI/Codex 使用的工程约束入口。执行任务时必须优先遵守本文件；若与用户临时要求冲突，应先说明风险并请求确认。

## 读取原则

- 必须先读取当前项目根目录的 `AGENTS.md`、`README.md`、`CHANGELOG.md`、`docs/decisions.md`，若文件存在。
- 禁止一次性把所有长文档塞入上下文；只读取当前任务相关章节。
- 若项目内存在更细规则，如 `RULES_FOR_AI.md`、`CODING_STANDARDS.md`、`docs/codex/*.md`，必须按需引用。

## 编码与文件

- 所有文本文件必须使用 UTF-8 without BOM。
- Python、JSON、YAML、Markdown、TXT 文件必须使用 LF 换行。
- 禁止写入乱码、GBK、GB2312、混乱编码内容。
- 文件路径必须使用 `pathlib.Path` 或标准路径 API，禁止硬编码路径分隔符。
- 修改文件前必须先检查现有内容和项目风格；不得覆盖用户未要求覆盖的改动。

## 配置与密钥

- 禁止在代码中硬编码 API Key、Token、密码、私钥、数据库连接串。
- 所有环境相关配置必须来自环境变量、`.env`、配置文件或密钥管理服务。
- 必须提供 `.env.example`，只包含假值。
- 配置优先级为：环境变量 > 配置文件 > 默认值。
- 程序启动时必须校验必需配置，缺失时给出清晰错误。

## 代码质量

- 禁止可变全局状态；只允许不可变常量。
- 禁止可变默认参数。
- 函数必须单一职责，过长或嵌套过深时必须拆分。
- 重复逻辑必须抽取为函数、类或配置。
- 禁止无意义注释；复杂业务规则必须解释“为什么”。
- 禁止保留未使用变量、未使用导入、死代码和大段注释旧代码。

## 数据与状态

- 状态必须显式传递，禁止隐式共享。
- 可能返回 `None` 的函数，调用方必须处理。
- 修改可变数据前必须确认是否需要拷贝。
- 时间存储和计算统一使用 UTC，展示时再转换为本地时区。
- JSON/YAML 读写必须显式处理异常；输出中文时保留原文可读性。

## 错误与日志

- I/O、网络、数据库、文件操作必须捕获具体异常，禁止裸 `except:`。
- 重新抛出异常时必须保留上下文。
- 使用 `logging`，禁止用 `print` 作为正式日志。
- 日志必须包含足够上下文，但禁止输出密钥、Token、密码等敏感信息。
- 外部依赖失败时优先降级、重试或给出清晰错误，禁止静默失败。

## 依赖与外部调用

- 依赖版本必须尽量锁定，避免 `latest`。
- 必须使用虚拟环境或项目内运行时，禁止污染全局环境。
- HTTP 请求必须设置超时。
- 网络重试必须有次数上限和退避策略。
- 数据库、Redis 等连接必须使用连接池或统一连接管理。
- 不要重复造已有成熟库能稳定解决的问题。

## 安全

- 所有外部输入必须校验类型、长度、范围和格式。
- SQL 必须参数化或使用 ORM，禁止字符串拼接 SQL。
- 禁止用 `eval`、`exec`、`os.system` 处理用户输入。
- 用户路径必须规范化后检查是否位于允许根目录内。
- 禁止用 `pickle` 处理不可信数据。
- 临时文件必须使用安全临时文件 API。

## 测试与可观测性

- 关键逻辑必须有最小测试或可运行自检。
- 项目应提供快速冒烟测试，覆盖最核心流程。
- Web 服务应提供健康检查；脚本应提供可验证运行方式。
- 关键操作应记录耗时、错误和必要上下文。
- 修改后必须说明已运行或未能运行的验证。

## 架构与熵增控制

- 代码应保持分层：入口层、业务层、数据层职责清晰。
- 跨层调用只能从上到下，禁止反向依赖。
- 模块间尽量依赖接口或事件，不直接写入他人状态。
- 新功能应优先通过配置开关控制。
- 不得为了短期可跑而破坏长期结构。
- 若规则与现有项目实际冲突，必须先说明冲突，再给出最小风险方案。

## 部署与环境

- 启动方式必须明确，最好提供一键启动脚本。
- 路径、端口、数据目录、日志目录必须可配置。
- 非交互运行时禁止依赖 `input()`。
- 日志写文件时必须考虑轮转，避免磁盘占满。
- 程序应能优雅关闭并释放资源。

## 协作与文档

- README 必须说明项目用途、启动方式、配置项和常见问题。
- 重要设计决策必须写入 `docs/decisions.md` 或同类文档。
- 大改后必须更新 `CHANGELOG.md` 或迭代记录。
- AI 生成或修改重要内容后，必须给出变更摘要、风险点和验证结果。

## Vibe Coding 专用规则

- 单次改动应尽量小而完整，避免一次生成过多文件。
- 若任务过大，必须拆阶段执行。
- 需求模糊、风险高、会删除/覆盖数据时，必须先确认。
- 不得为了省事跳过备份、检查、验证。
- 本规则优先级高于代码简洁性；如无法满足，必须说明原因和替代方案。
