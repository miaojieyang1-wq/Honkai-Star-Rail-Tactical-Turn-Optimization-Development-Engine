# 变更日志

## 2026-05-17

### 修复

- `BattleEngine.plan_optimal()` 现在会把调用参数 `gamma` 传递到搜索层和受击分支剩余窗口搜索，避免参数只出现在报告假设中。
- `config_loader.get_env()` 改为优先读取系统环境变量，再回退到 `.env`，符合配置优先级约束。
- 默认动作转移现在会读取 `ActionDefinition.sp_cost` 写入的动作元数据，避免自定义技能消耗只判定、不生效。
- `ActionDefinition` 新增 `toughness_damage`，默认动作转移可通过事件链真正削减目标韧性。
- 默认动作转移只在 `ActionDefinition.energy_cost` 显式配置时消耗能量，`requires_full_energy` 仅作为合法性门槛，避免引入隐式数学假设。
- `ActionDefinition` 会拒绝负 `sp_cost`、负 `energy_cost` 和负 `toughness_damage`，避免非法元数据进入状态转移。
- 伤害结算值和搜索剪枝伤害上界会拒绝负值，保证 `D_acc` 与乐观上界保持在伤害数值域内。
- 搜索控制参数会校验公式域：`delta_t >= 0`、`0 < gamma <= 1`、受击分支数非负。
- 受击分支会校验回能、权重、概率和仇恨标签数值域，避免负权重或越界概率进入分支分析。
- `ActionDefinition.sp_cost` 必须是非负整数，避免离散战技点成本被小数截断。
- 状态层会拒绝负 `E_max`、负 `SP_max`、负 `T_max` 和负削韧量，保护七元组状态变量的数学域。
- `SP_t`、`SP_max` 和 SP 事件数量必须是非负整数，避免离散战技点状态被小数截断。
- 行动队列会拒绝负值、无穷值和 NaN 行动值，保护行动值窗口的时间轴语义。
- `ActionQueue` 初始化也会校验行动值，避免绕过写入口注入非法时间轴坐标。
- 行动队列会拒绝非 `UnitType` 的单位类型，避免非法队列节点被搜索窗口静默忽略。
- 搜索评分配置会校验公式域：资源惩罚系数非负，安全能量比例位于 `[0, 1]`。

### 工程约束

- 新增项目 AI 协作入口 `AGENTS.md`，让后续 Codex/AI 会话可直接读取安全规则。
- 冒烟测试补充 `gamma` 传递、环境变量优先级、自定义技能 SP 消耗、自定义削韧、显式能量成本、动作定义数值域、伤害数值域、搜索参数域、受击分支数值域、离散 SP 成本、状态向量数值域、SP 状态整数域、行动值数值域、队列初始化校验、行动队列单位类型和搜索配置域覆盖。

## 2026-05-16

### 新增

- 新增 `external_api.py`，定义速度轴模拟器、即时伤害计算器、外部 API 包和本地 Mock 实现。
- 新增 `tactical_report.py`，将 `TacticalAdvice` 转换为战术建议书，并支持可替换 LLM 客户端。
- `BattleEngine` 新增 `external_api_bundle` 参数，可将伤害计算 API 注入搜索评分和剪枝上界。
- 冒烟测试新增外部 API 与战术报告闭环验证。

### 文档

- README 补充玩家痛点、外部 API、LLM 输出和验证命令。
- ARCHITECTURE 补充五层架构、外部 API 接口层和 LLM 战术建议书层。

### 边界说明

- 当前 Mock 仅验证架构和接口形状，不代表真实游戏伤害公式、仇恨公式或角色机制参数。
